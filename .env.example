# ---------------------------------------------------------------------------
# Site institucional ness. — variáveis de ambiente
# Copie para .env e preencha os valores.
# NUNCA commite o .env com valores reais.
# ---------------------------------------------------------------------------

# VITE_CANAL_BASE_URL
# Deixe VAZIO (comentado) em desenvolvimento local e em produção CF Pages.
# As chamadas /api/* são tratadas por:
#   - Dev:        Express proxy em server.ts
#   - Produção:   CF Pages Functions em functions/api/[[route]].ts
#
# Só defina se quiser que o browser chame o canal diretamente
# (exige CORS configurado no Worker).
#
# VITE_CANAL_BASE_URL=

# VITE_BRAND: Controla o tema da marca (ness | trustness | forense)
# VITE_BRAND=ness
