import Services from '../pages/Services';

/** Services também é seção da home; como rota própria, roda em modo standalone. */
export default function ServicesRoute() {
  return <Services standalone />;
}
