import Verticals from '../pages/Verticals';

/** Verticals também é seção da home; como rota própria, roda em modo standalone. */
export default function VerticalsRoute() {
  return <Verticals standalone />;
}
