import Solutions from '../pages/Solutions';

/** Solutions também é seção da home; como rota própria, roda em modo standalone. */
export default function SolutionsRoute() {
  return <Solutions standalone />;
}
